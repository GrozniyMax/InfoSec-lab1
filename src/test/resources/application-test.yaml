spring:
  datasource:
    url: jdbc:h2:mem:lab1;DB_CLOSE_DELAY=-1;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  flyway:
    enabled: true

  sql:
    init:
      mode: never